P27，文件相关，文件读写，重定向
数组赋值：memcpy(b,a,sizeof(a))     memcpy(b,a,sizeof(int)*k)  (浮点型int换成double)   记得加上#include <string.h>
memset(a,0,sizeof(a))，把a的初始值都赋成0
很多情况下，最好是在做一件事之前检查是不是可以做，而不要做完再后悔，悔棋很麻烦。（比如，先判断，再移动）
printf输出，fprintf输出到文件，sprintf输出到字符串（后两个代码写法一样）
strchr在一个字符串中查找单个字符 strchr(s,buff[i])，在s这个字符串中查找buff[i]这个字符
scanf遇到空格或者tab会停下来
善用常量数组往往能简化代码。定义常量数组时无需指明大小，编译器会计算。（char s[]="1244554657sdfdsfa"这样子。然后如果有超过数组长度的i，s[i]是空，可以用这个判断）
头文件ctype.h中定义的isalpha、isdigit、isprint等工具可以用来判断字符的属性，而toupper、tolower等工具可以用来转换大小写
(有时候代码写的啰嗦，没事，先写出来，然后优化，比如可能发现好几个循环能合成一个)


不要排斥for循环，有时可能会很好用。需要排斥的是重复遍历，就是争取O(n)这样子，只遍历一遍；需要排斥O(n*m)这种


floor(x+0.5)  这个的作用是对x进行四舍五入。能够避免浮点误差（比如，1开根号变成了0.999999，floor一下变成0了，这就是浮点误差。而用这种，就不会出现这种情况。另，先别钻牛角尖，不要问我，1.5是1.49999的情况......这种办法广为接受的话就先用着，但是这个问题，我留着，嗯）

较大的数组放在main外，防止栈溢出
