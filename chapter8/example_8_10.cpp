

const int maxn=505;
int save[maxn];


int main()
{
    int kase;
    cin>>kase;
    while(kase--)
    {
        memset(save,0,sizeof(save));
        int n,k;
        cin>>n>>k;
        for(int i=0;i<n;++i)
            scanf("%d",&save[i]);

    }
}
